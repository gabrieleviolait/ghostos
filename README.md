# GhostOS

Versione corrente: v0.5.5

Ghost OS e' un piccolo sistema operativo educativo in Assembly NASM
16-bit real mode. Il kernel e' diviso in moduli `.inc`, la shell include
una calcolatrice multi-digit, terminale VGA con scrolling e una dashboard
TUI a pannelli tramite `gui` / `desktop`.
